@model mfc.web.Models.OrganizationTypeModel

@section workmenu {
    @Html.Partial("_AdminMenu", 1)
}

<h3>Удалить тип ОГВ?</h3>
@using (Html.BeginForm("Delete", "OrganizationType", FormMethod.Post, new { @class = "form" })) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)
<fieldset>
    <legend>OrganizationTypeModel</legend>
    @Html.HiddenFor(model => model.Id)
    @Html.HiddenFor(model => model.Caption)

    <div class="display-field">
        @Html.DisplayFor(model => model.Caption)
    </div>
</fieldset>

    
    <p>
        <input type="submit" value="Удалить" class="btn btn-lg btn-default" />
    </p>
}

<p>@Html.ActionLink("Вернуться к списку", "List")</p>
