@model mfc.web.Models.ActionTypeModel

@section workmenu {
    @Html.Partial("_AdminMenu", 3)
}

@{
    var for_edit = Model.Id > 0;
    
    if (for_edit) {
        ViewBag.Title = "Редактирование вида деятельности";
    }
    else {
        ViewBag.Title = "Добавление вида деятельности";
    }
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@ViewBag.Title</h2>

@using (Html.BeginForm(for_edit ? "Edit" : "Create", "ActionType", FormMethod.Post, new { @class = "form" })) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>ActionTypeModel</legend>

        @Html.HiddenFor(model => model.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.Caption)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Caption)
            @Html.ValidationMessageFor(model => model.Caption)
        </div>
        <div class="editor-label">
            @Html.LabelFor(model => model.NeedMakeFile)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.NeedMakeFile)
            @Html.ValidationMessageFor(model => model.NeedMakeFile)
         </div>
        <p>
            <input type="submit" value="Сохранить" class="btn btn-lg btn-default" />
        </p>
</fieldset>
}

<div>
    @Html.ActionLink("Вернуться к списку", "List")
</div>
