@model mfc.web.Models.ServiceListViewModel

@{
    ViewBag.Title = "Справочник услуг";
    Layout = "~/Views/Shared/_Layout.cshtml";
    Int64 last_org = -1; 
    bool render_hr = false;
}

<script>
    $(function () {
        $('select#organization').bind('change', function () {
            var val = $(this).val();
        });
    });
</script>

@section featured {
    <section class="featured">
        <div class="content-wrapper">
            <hgroup class="title"><h2>@ViewBag.Title</h2></hgroup>
            <p>
                @using (Html.BeginForm()) {
                    <div style="margin: 0 10px;float:left">
                        ОГВ: @Html.DropDownListFor(m => m.OrganizationId, new SelectList(Model.Organizations, "Id", "Caption"), new { Id = "organization" })
                    </div>
                    <div style="clear:both" />
                }
            </p>
        </div>
    </section>
}
<p>
    @Html.ActionLink("Новый", "Create")
</p>
<table class="tableizer-table">
    <tr>
        <th>
            Название
        </th>
        <th>
            ОГВ
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model.Services) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Caption)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Organization.Caption)
            </td>
            <td>
                @Html.ActionLink("Изменить", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Удалить", "Delete", new { id = item.Id })
            </td>
        </tr>

    }

</table>
